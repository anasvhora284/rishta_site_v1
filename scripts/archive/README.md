# Archived one-off scripts

Historical Excel import, city cleanup, and batch SQL tools were removed from the active repo after the production data migration (June 2026).

Full copies live in:

`/home/ayaan/Work/RishtaSiteBackups/rishta-site-*/project/rishta-portal/scripts/`

Active maintenance scripts remain in `scripts/` (DB backup, archive verify, reference-data rebuild).

**Keep** `supabase/migrations/` — that is the live schema history for Supabase, not disposable import tooling.
